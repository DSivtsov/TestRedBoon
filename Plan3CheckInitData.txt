interface IPathFinder
{
IEnumerable<Vector2> GetPath(Vector2 A, Vector2 C, IEnumerable<Edge> edges);
}

struct Rectangle
{
public Vector2 Min;
public Vector2 Max;
}
struct Edge
{
public Rectangle First;
public Rectangle Second;
public Vector3 Start;
public Vector3 End;
}

--------------------------//----------------------------------
Corect the Point created by Instantiate in Create Rec and in Checkinn Data (Wrong Z)???
--------------------------//----------------------------------
CheckExistOverlapingRectangle
	
	GetListAllRectangles
	for i = 0 to lenght - 1
		for j = i +1 to end lenght
			if( ExistOverlaping(list[i],list[j]))
				return true
	return false

Additional Checks:
	After CheckExistOverlapingRectangle()
	
	Check startPointFindPath and endPointFindPath must be not part of Edge (first or last)
		because in that case the first or last edges not usefulness and can be skipped (thrown)
	
	Check Corect Edge (absent space between first and seconf rec)
		If Contains in first and second rec
		Dots StartEdge and EndEdge correctly set the its right order
	
	Before 	CheckExistOverlapingRectangle CheckAllRectangleCorrect
		the Min & Max is thr BootomLeft and TopRight
	
	Check PointFindPath (Can Exist Flag IsPointsFindPathMustBeOnEdgeRectanle - yes/no )
		startPointFindPath and endPointFindPath Contains in first and last Rectangle